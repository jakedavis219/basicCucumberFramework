# basicCucumberFramework

-Make sure you have NPM & Node installed (npm -v, node -v)
-Clone to your repo
-npm install
-Kick off the tests with either "npm test" or "node ./node_modules/@cucumber/cucumber/bin/cucumber-js"
